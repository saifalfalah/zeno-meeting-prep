# Database (Neon Postgres)
# Get your connection string from https://neon.tech
DATABASE_URL="postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require"
# Use the -pooler connection string for production (recommended for Vercel)
DATABASE_URL_POOLER="postgresql://user:password@ep-xxx-pooler.region.aws.neon.tech/dbname?sslmode=require"

# NextAuth v5 Configuration
# Generate secret with: openssl rand -base64 32
AUTH_URL="http://localhost:3000"
AUTH_SECRET="your-secret-here"

# Google OAuth & Calendar API
# Create credentials at https://console.cloud.google.com/
# Required scopes: calendar, calendar.events, userinfo.email, userinfo.profile
AUTH_GOOGLE_ID="your-client-id.apps.googleusercontent.com"
AUTH_GOOGLE_SECRET="your-client-secret"

# Perplexity API
# Get API key from https://www.perplexity.ai/
# Using sonar-pro model with enhanced search parameters for high-quality prospect research
PERPLEXITY_API_KEY="pplx-xxxxxxxxxxxx"

# Anthropic Claude API
# Get API key from https://console.anthropic.com/
ANTHROPIC_API_KEY="sk-ant-xxxxxxxxxxxx"

# Inngest (Background Jobs)
# Create account at https://inngest.com
INNGEST_EVENT_KEY="your-inngest-event-key"
INNGEST_SIGNING_KEY="your-inngest-signing-key"

# Application
NODE_ENV="development"
# Used for webhook callbacks (must be publicly accessible in production)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
